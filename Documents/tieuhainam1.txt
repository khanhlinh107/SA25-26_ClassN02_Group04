Activity 1: Chia hệ thống theo microservices, ko theo UI
| **Business Capability**            | **Proposed Microservice** | **Data Owned (Entities)**                                       |
| ---------------------------------- | ------------------------- | --------------------------------------------------------------- |
| **User Management**                | **User Service**          | User Profile, Role (Admin/Student), Student Information         |
| **Authentication & Authorization** | **Auth Service**          | Authentication Credentials, Access Tokens, User Roles           |
| **Book Catalog Management**        | **Book Service**          | Book Details, Author, Category, Availability Status, Image URLs |
| **Borrowing & Returning**          | **Borrow Service**        | Borrow Records, Borrow Date, Due Date, Return Status            |
| **Overdue Management**             | **Overdue Service**       | Overdue Records, Penalty Status, Due Date Calculations          |
| **Notification Management**        | **Notification Service**  | Notification Logs, Message Templates, Delivery Status           |


3. Define External Dependencies

Hệ thống thư viện cần tương tác với các external systems sau:

External System	Purpose
Firebase Authentication	Xác thực người dùng và phân quyền
Firebase Firestore / Storage	Lưu trữ dữ liệu sách, người dùng, ảnh bìa
Firebase Cloud Messaging (FCM)	Gửi thông báo hạn trả, quá hạn
Email Service (Gmail / SendGrid)	Gửi email nhắc trả sách
External Book API (Google Books API)	Lấy thông tin và ảnh sách tự động


Activity 2:
| Endpoint          | HTTP Method | Description                              | Data Returned                                       |
| ----------------- | ----------- | ---------------------------------------- | --------------------------------------------------- |
| `/api/books/{id}` | GET         | Retrieve full details of a specific book | Book object (title, author, category, availability) |
| `/api/books`      | GET         | Search or list books based on criteria   | List of Book objects (summary view)                 |
| `/api/books`      | POST        | Add a new book (Admin only)              | Created Book object                                 |
| `/api/books/{id}` | PUT         | Update book information (Admin only)     | Updated Book object                                 |


2. Define the Borrow Service’s Interaction (Consumer)
🎓 Borrow Service cần gì từ Book Service khi mượn sách?

Khi Student mượn sách, Borrow Service cần:

bookId

title

availability status

👉 Borrow Service KHÔNG lưu thông tin chi tiết sách lâu dài
👉 Borrow Service CHỈ gọi API của Book Service